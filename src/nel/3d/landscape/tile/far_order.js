import Enum from "nel/misc/enum";

/**
 * @namespace nl3d.landscape
 * @enum
 */
var FarOrder = new Enum({
    order0: 0,
    order1: 1,
    order2: 2,
    count: undefined
});

export default FarOrder;
