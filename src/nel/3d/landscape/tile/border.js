import Enum from "nel/misc/enum";

/**
 * @namespace nl3d.landscape
 * @enum
 */
var Border = new Enum({
    top: 0,
    bottom: undefined,
    left: undefined,
    right: undefined,
    count: undefined
});

export default Border;
