import Enum from "nel/misc/enum";

/**
 * @namespace nel.3d.landscape
 * @enum
 */
var Bitmap = new Enum({
    diffuse: 0,
    additive: undefined,
    alpha: undefined,
    count: undefined
});

export default Bitmap;
